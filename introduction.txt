ZhiYang: 
- 测试功能
- 目前这个 Local Repo 是用 git clone <url> 写成的
- 使用 git branch <branch-name>; git checkout <branch-name>; git push 来测试修改内容
- 使用 git branch -D <local-branch-name> 删除 Local branch
- 使用 git push origin -d <remote-branch-name> 删除 remote branch
- 在 765baf0 新增一个 Commit

